// script.js
const librosDiv = document.getElementById('libros');
const usuariosDiv = document.getElementById('usuarios');

const libros = [];
const usuarios = [];

function agregarLibro(titulo, autor, isbn) {
    // Lógica para agregar el libro al array de libros y actualizar la interfaz
}

function registrarPrestamo(libroId, usuarioId, fechaPrestamo) {
    // Lógica para registrar el préstamo y calcular la fecha de devolución
}
